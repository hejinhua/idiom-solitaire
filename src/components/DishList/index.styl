.list {
}
.list-item {
  margin-top: 32px;
  color: #323334;
}
.face {
  width: 172px;
  height: 172px;
  border-radius: 4px;
  overflow: hidden;
  margin-right: 15px;
  background-color: #f5f5f5;
}
.name-wrapper {
  display: flex;
}
.name {
  font-size: 28px;
  font-weight: bold;
  line-height: 40px;
}
.desc {
  margin-top: 8px;  
  font-size: 18px;
  line-height: 25px;
}
.new {
  background: url('../../assets/icons/new-tag.png') no-repeat;
  background-size: 100% 100%;
  width: 48px;
  min-width: 48px;
  height: 28px;
  margin: 6px;
  color: #fff;
  font-size: 20px;
  line-height: 28px;
  text-align: center;
}