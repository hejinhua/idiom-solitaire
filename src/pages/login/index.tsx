import React, { useCallback } from 'react'
import { View, Text, Button } from '@tarojs/components'

import './index.styl'

const Index = () => {
  return <View className='wrapper'>登录</View>
}

export default Index
